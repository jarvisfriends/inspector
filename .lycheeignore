# Links lychee should not verify.
#
#
# The demo gif used to be listed here: it is a build artifact attached to the
# release by goreleaser, so the README links it as
# releases/latest/download/demo.gif, which 404'd while every release was
# failing on a dirty tree. v0.0.13 is the first release to carry the asset, so
# the link resolves and a broken gallery link is a real failure again.

# contributor-covenant.org TCP-resets link checkers run from GitHub-hosted
# runners (curl/hyper "connection reset by peer", os error 104) no matter the
# user agent; the canonical Code of Conduct URL renders fine in a browser.
# Re-check occasionally and drop this line when the host stops blocking.
https://www.contributor-covenant.org/version/2/1/code_of_conduct/
